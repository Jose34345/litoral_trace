name: Temporary Public Final Gates Dispatcher

on:
  push:
    branches:
      - feature/smart-import-v1
    paths:
      - ".github/workflows/temp-public-final-gates-dispatch.yml"

permissions:
  actions: write
  contents: read

concurrency:
  group: temp-public-final-gates-dispatch
  cancel-in-progress: true

jobs:
  dispatch-final-gates:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Dispatch selected final gates on Smart Import branch
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          TARGET_REF: feature/smart-import-v1
        shell: bash
        run: |
          set -euo pipefail
          # Dispatch only the final integration surfaces needed for this PR.
          for workflow in \
            ci.yml \
            release-integration-gates.yml \
            v1-final-release-acceptance.yml
          do
            gh workflow run "$workflow" \
              --repo "$GH_REPO" \
              --ref "$TARGET_REF"
          done
