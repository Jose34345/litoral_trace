[alembic]
# path to migration scripts
script_location = alembic
# sys.path prefix, relative to current working directory.
prepend_sys_path = src
# template used to generate migration files
# file_template = %%(rev)s_%%(slug)s
# timezone to use when rendering dates
timezone = utc

[alembic:runtime]
# sqlalchemy.url se resuelve en alembic/env.py con prioridad:
# MIGRATION_DATABASE_URL -> DATABASE_URL
sqlalchemy.url =

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = INFO
handlers = console

[logger_sqlalchemy]
level = WARN
handlers = console
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers = console
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stdout,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(asctime)s %(levelname)-5.5s [%(name)s] %(message)s

[post_write_hooks]
# hooks can be configured here if needed
